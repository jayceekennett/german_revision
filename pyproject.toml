[project]
name = "german-revision"
version = "0.2.0"
authors = [
    { name = "Jaycee Kennett" }
]

license = { text = "MIT" }

[project.scripts]
german-revision = "german_revision.main:main"

[tool.setuptools.package-data]
german_revision = ["data/*.csv"]






